# ===========================================
# Environment Variables Configuration
# ===========================================

# Database (Supabase PostgreSQL)
# Get these from: Supabase Dashboard > Settings > Database
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres"

# Authentication
JWT_SECRET="your-super-secret-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# Next.js
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# ===========================================
# Production Environment Settings
# ===========================================
# For production deployment, ensure the following:
#
# 1. DATABASE_URL & DIRECT_URL (Supabase)
#    - DATABASE_URL: Connection pooling URL (port 6543, with pgbouncer=true)
#    - DIRECT_URL: Direct connection URL (port 5432, for migrations)
#    - Get both from: Supabase Dashboard > Settings > Database > Connection string
#
# 2. JWT_SECRET
#    - Use a cryptographically secure random string (minimum 32 characters)
#    - Generate with: openssl rand -base64 32
#    - Never commit the actual production secret to version control
#
# 3. NEXT_PUBLIC_API_URL
#    - Set to your production domain (e.g., https://yourdomain.vercel.app/api)
#    - Ensure HTTPS is used in production
#
# 4. Security Recommendations
#    - All secrets should be set via environment variables in your hosting platform
#    - Supabase automatically handles SSL/TLS connections
#    - Rotate secrets periodically
#    - Monitor for unauthorized access attempts
#
# ===========================================
